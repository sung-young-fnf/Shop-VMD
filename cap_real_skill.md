---
name: cap-real
description: 상품 사진을 기준으로 원단과 자수를 반영한 곡면 Three.js 모자를 재구성하고, 회전 가능한 웹 뷰어에서 검증한다. 사진 기반 야구모자 제작과 기존 모자 모델의 다른 프로젝트 재사용에 적용한다.
---

# Cap Real — 사진 기반 Three.js 모자 제작

## 1. 목적과 적용 범위

사용자가 제공한 모자 사진에서 **형태, 원단 결, 로고, 봉제 구조**를 읽고 실제 곡면 메시로 만든다. 정면뿐 아니라 측면·뒷면·안쪽으로 회전해도 연결된 모자로 보여야 한다.

이 문서의 기준 구현은 `cap_reference_viewer`의 네이비 NY 6패널 캡이다. 2026-09-08 브라우저 검증과 독립 기능·외관 검토를 통과했고, 사용자가 틀을 긍정적으로 평가했다. 이 평가는 사진과의 완전 일치, 실측 정확도, 물성 복원을 뜻하지 않는다.

- 우선 적용: 코튼 트윌 야구모자, 곡선 챙, 패널 봉제, 뒤 조절 스트랩.
- 다른 구조: 스냅백·메시 캡·버킷햇은 해당 구조를 새로 분석한다. 6패널 틀을 그대로 강제하지 않는다.
- 산출물: 모자 생성 코드, 파생 텍스처, 관찰용 웹 화면, 출처 기록, 실제 렌더 검증 자료.
- 실행 범위: 사용자가 지정한 작업 폴더와 앱 경계를 따른다. 운영 반영이나 배포는 별도 요청 범위다.

이 파일은 요청한 이름의 재사용 문서다. 자동 탐색되는 스킬로 설치할 때에는 지원되는 스킬 디렉터리에 `cap-real/SKILL.md`로 배치한다. 문서 작성 자체가 전역 스킬 설치를 의미하지 않는다.

## 2. 시작할 때 필요한 입력

| 입력 | 용도 | 부족할 때의 처리 |
|---|---|---|
| 상품 정면 사진 | 폭·높이 비율, 챙의 좌우 굽힘, 로고 크기·위치 | 사선 사진으로 추정하고 정면 정확도를 제한 |
| 상품 측면 사진 | 앞뒤 깊이, 앞판 기울기, 챙 길이·낙차 | 깊이를 추정값으로 기록 |
| 상품 뒷면 사진 | 개구부, 스트랩, 버클, 뒤 로고 | 보이지 않는 구조를 확정하지 않음 |
| 원단 확대 사진 | 직조 방향·밀도·대비, 봉제실 | 보이는 범위만 재현; 정밀 재질 일치 주장 금지 |
| 안쪽 사진 | 스웨트밴드, 패널 테이프, 라벨, 안쪽 리벳 | 내부 구현 범위를 명시 |
| 사선 상품 사진 | 전체 인상과 연결부 비교 | 다른 시점으로 보완 |
| 실제 치수 또는 목표 크기 | 월드 단위, 매장 배치·착용 크기 | 정규화된 상대 치수로 제작 |
| 출력 경로와 사용 방식 | 단독 뷰어, 기존 앱 통합, 모델 내보내기 | 기존 요청·프로젝트 설정에서 결정 |

모든 사진이 같은 SKU와 색상인지 확인한다. 착용 사진은 실루엣 보조 자료로 사용하며 인물·머리카락을 상품 표면에 투영하지 않는다. 자료가 부족해도 확인된 면의 구현은 진행하되 추정한 면을 기록한다.

## 3. 도구와 관련 지침

### 실행에 필요한 도구

- Node.js와 패키지 관리자: 독립 설치와 반복 가능한 실행.
- Three.js: 곡면 `BufferGeometry`, 재질, 조명, 카메라, `OrbitControls`.
- TypeScript와 Vite: 기준 사례의 개발·빌드 구성. 다른 프로젝트에서는 기존 스택을 유지할 수 있다.
- Sharp 또는 동등한 로컬 이미지 처리 도구: 사진 자르기, 알파 처리, 텍스처 파생, 해시 기록.
- 실제 브라우저 자동화 도구: 화면 캡처, 회전·확대·시점 버튼과 오류 확인. 기준 사례는 Playwright와 설치된 Chrome을 사용했다.
- 원본과 결과를 직접 열어 볼 이미지 뷰어.

기준 사례에서 사용한 버전은 `three 0.183.2`, `@types/three 0.183.1`, `typescript 5.9.3`, `vite 7.3.1`, `sharp 0.34.5`, `playwright 1.58.2`다. 검증된 조합의 기록이며 최신 버전 권장은 아니다. 기존 프로젝트의 의존성과 잠금 파일을 먼저 확인한다.

### 환경에 있을 때 참고할 스킬

| 지침 | 적용 목적 |
|---|---|
| `product-photo-realism` | 실제 SKU 사진 활용, 사진 조명과 물성의 구분, 곡면 연결부 검증 |
| `img2threejs` | 사용자가 해당 워크플로를 요청했을 때 스펙·단계별 하네스 수행 |
| 프런트엔드 지침 | 관찰 UI와 반응형 화면 제작 |
| 시각 QA 지침 | 원본·렌더 직접 비교와 실제 브라우저 조작 검증 |
| TypeScript 개발 지침 | 타입 검사와 기존 코드 규칙 준수 |
| 디버깅 지침 | 런타임 오류가 있을 때 재현·원인 확인·수정 |

스킬 이름은 특정 환경의 예시다. 다른 환경에서는 아래 제작 절차를 직접 수행할 수 있다. **이 최종 기준 모델은 img2 하네스 전체를 실행한 결과가 아니다.** 중단된 이전 작업의 하네스 결과를 이 모델의 통과 근거로 사용하지 않는다.

## 4. 권장 파일 구성과 이동 방법

```text
<output>/
  src/
    surfaces.ts       곡면 수식, 메시 생성, 표면 위치 계산
    cap.ts            패널·챙·봉제·스트랩·로고 조립
    materials.ts      사진 로딩, 색 공간, 원단·금속·로고 재질
    interior.ts       내부 테이프 표현
    main.ts           관찰 화면, 조명·카메라·입력
    style.css
  public/assets/      원본과 분리한 파생 텍스처·비교 사진
  scripts/
    assets.mjs        SKU별 이미지 파생 절차
    capture.mjs       실제 브라우저 캡처와 조작
    compare.mjs       비교용 정면 크롭
  evidence/           출처·해시·캡처·검증 결과
  package.json
  pnpm-lock.yaml
  tsconfig.json
  index.html
```

현재 코드와 증거는 이 문서 옆의 [cap_reference_viewer](cap_reference_viewer/)에 있다. 문서만 다른 곳에 가져가면 절차를 따라 새로 구현할 수 있고, **현재 틀을 바로 재사용하려면 해당 소스·에셋·패키지 구성도 함께 가져간다.** `node_modules`는 대상 폴더에서 설치한다.

`scripts/assets.mjs`의 원본 경로와 크롭 좌표는 현재 SKU 전용이다. 다른 사진에 그대로 실행하지 않는다. `materials.ts`와 `main.ts`의 `/assets/` 경로도 대상 앱의 배포 경로에 맞춘다.

## 5. 제작 절차

### A. 레퍼런스 분석과 출처 보존

1. 상품 사진을 직접 열고 정면·측면·뒷면·안쪽·디테일 역할을 배정한다.
2. 해상도, 이미지 방향, 알파 유무, SKU, 출처, 파일 SHA-256을 기록한다.
3. 패널 개수, 챙 곡률, 로고 비율, 개구부와 버클 위치를 정리한다.
4. 원본은 수정하지 않고 출력 폴더에 크롭·텍스처·비교용 축소본을 생성한다.

투명 PNG는 알파 밖 RGB에 깨진 줄무늬나 배경색이 남을 수 있다. 비교용 사진은 알파를 존중해 합성하고, 텍스처는 유효한 상품 영역만 추출한다. 촬영 그림자를 모자의 일부로 포함하지 않는다.

### B. 재질보다 먼저 틀 맞추기

1. 좌표계를 정한다. 기준 구현은 `+Y` 위, `+Z` 정면·챙, `-Z` 뒷면, `X` 좌우다.
2. 크라운과 챙을 중립 재질로 만든 뒤 정면·측면·사선에서 확인한다.
3. 크라운 폭·높이, 위쪽 어깨 폭, 정면 하단의 아치, 챙 길이·좌우 굽힘을 맞춘다.
4. 뒤 개구부는 실제 메시 경계를 올려 만든다. 흰색 칠이나 배경 이미지로 구멍을 흉내 내지 않는다.
5. 챙의 시작 경계를 크라운 앞쪽 하단과 공유하거나 같은 표면식에서 계산한다.
6. 작은 장식을 추가하기 전에 연결부의 틈과 정면·측면 실루엣을 수정한다.

카메라 원근과 물체 형상을 구분한다. 한 사선 사진에 맞추려고 한쪽 챙만 비대칭으로 꺾지 않는다. 실측값이 없으면 비율을 기록하며 실제 제품 치수라고 표시하지 않는다.

### C. 실제 사진에서 재질 만들기

1. 확대 사진에서 로고·봉제선·눈에 띄는 그림자가 없는 원단 영역을 고른다.
2. 직조 방향을 유지하며 타일 경계를 처리한다. 경계 혼합은 후보 기법이며 무조건 무봉제 텍스처가 되는 것은 아니다.
3. 근거리와 일반 관찰 거리에서 반복무늬, 직조 크기, 모아레를 확인한다.
4. 원단 자체의 색과 명암을 보존하면서 과도하게 굵고 강한 결만 조정한다.
5. 원단, 봉제실, 안쪽 밴드, 금속, 로고 재질을 분리한다.

코튼이라는 이유만으로 광택을 추가하지 않는다. 사진에 보이는 밝은 결이 촬영 조명인지 섬유 반사인지 구분한다. 현재 사례는 원단 `roughness: 1`, `metalness: 0`으로 시작했고 추가 sheen·clearcoat를 사용하지 않았다. 다른 소재에는 사진 근거에 맞춰 다시 판단한다.

Base Color 사진은 sRGB로 처리한다. 실제 normal·roughness·AO 맵을 사용한다면 각 맵의 데이터 색 공간과 UV 채널을 설치된 Three.js 버전에 맞춰 확인한다. 단순 밝기 변환 결과를 측정된 물성 맵이라고 주장하지 않는다.

### D. 로고와 봉제 디테일

- 글자 모양을 임의 폰트로 대체하기보다 실제 상품 사진에서 자수를 추출한다.
- 배경 제거 임계값은 해당 원단·자수 대비에 맞춘다. 밝은 자수에 사용한 임계값을 유색·어두운 자수에 그대로 적용하지 않는다.
- 로고는 작은 곡면 패치로 만들고 크라운 표면 위치를 계산해 밀착시킨다.
- 정면·뒷면의 높이 곡선이 다르면 각각의 실제 표면을 사용한다. 정면 패치를 단순히 뒤집으면 뒷면에서 떠 보일 수 있다.
- UV 상하·좌우 방향을 정면과 뒷면에서 직접 확인한다.
- 패널선과 챙 스티치는 가는 곡선으로 배치한다. 두꺼운 파이프처럼 부풀리지 않는다.
- 버클·스트랩·밴드·내부 테이프는 사진에서 확인된 구조만 구현한다.

현재 로고는 사진의 실결과 명암을 보존한 곡면 알파 패치다. 실 한 가닥을 입체 모델링한 자수가 아니다. 큰 접사, 변형, 물리적 재조명이 요구되면 별도 입체 자수나 적절한 표면 맵 제작을 추가한다.

### E. 관찰 화면과 조명

- 모자 생성 함수는 `THREE.Group`을 반환하고, UI·카메라·렌더러는 바깥에서 소유한다.
- 정면, 사선, 측면, 뒷면, 안쪽, 원단 확대 시점을 제공한다.
- 드래그 회전, 휠·터치 확대, 시점 초기화를 연결한다.
- 원본 사진을 옆에서 바꿔 볼 수 있게 해 기억에 의존한 비교를 줄인다.
- 화면 크기 변경 시 렌더러 크기, 카메라 비율과 프레이밍을 갱신한다.
- 좁은 화면에서는 원본 패널을 아래로 배치하고 모자가 화면 밖으로 잘리지 않게 한다.
- 텍스처 로딩과 셰이더 준비 후 실제 프레임을 확인한다. 준비 플래그만 성공 근거로 삼지 않는다.

기준 뷰어는 사진 색 보존을 위해 sRGB 출력과 `NoToneMapping`을 사용했다. 원단은 `MeshStandardMaterial`, 사진 로고는 `MeshBasicMaterial`과 `toneMapped: false`를 사용했다. Basic 로고에는 촬영 명암이 남고 현재 조명에는 반응하지 않는다. 다른 앱의 톤 매핑·노출에서 이 조합을 그대로 옮기면 불일치가 생길 수 있으므로 실제 대상 장면에서 재검증한다.

## 6. 검증된 틀의 기준 수식과 값

아래는 **현재 NY 캡을 재현할 때의 출발점**이다. 다른 SKU의 표준 치수가 아니다. 전체 구현은 [surfaces.ts](cap_reference_viewer/src/surfaces.ts)와 [cap.ts](cap_reference_viewer/src/cap.ts)를 확인한다.

### 크라운

`theta`는 둘레 각도, `t`는 위에서 아래로 향하는 각도다. 보통 `0 < t <= π/2` 범위이며 뒤 개구부에서는 하단 각도를 줄인다. `inset`은 내부 테이프 등의 안쪽 오프셋이다.

```text
r = sin(t)^0.6
x = sin(theta) × r × (1 - inset)
z = cos(theta) × r × (1.05 - inset)
base = 0.02 + [-0.12 × sin(theta)^2
               + 0.24 × max(0, cos(theta))^2] × sin(t)^2
y = base + 1.18 × max(0, cos(t))^0.88
```

- `r`의 지수: 위쪽 어깨 폭과 돔 형태를 조절한다.
- `1.18`: 크라운의 수직 형태를 조절하는 기준 높이 항이다.
- `1.05`: 앞뒤 반경이다. 좌우 기준 반경은 1이다.
- `base`: 앞쪽 하단을 올리고 좌우를 낮춰 챙 연결 아치를 만든다.
- `base` 보정은 위로 갈수록 사라져야 한다. 위쪽까지 일정한 보정을 더하면 꼭짓점이 갈라지거나 뾰족해진다.
- 소수 지수 연산 전 `cos(t)`를 0 이상으로 제한한다. 경계의 작은 음수가 NaN이 되면 곡선·메시 생성이 실패할 수 있다.

현재 뒤 개구부는 `cos(theta) < 0`이고 `abs(sin(theta)) < 0.43`인 구간에 반원형 높이 항을 주어 하단 각도를 줄인다. 다른 모자로 바꿀 때는 개구부 폭·높이·스트랩 경계와 크라운 기본 높이의 일치를 다시 검사한다. 정확한 패턴 절단이나 방수 메시를 보장하는 식은 아니다.

### 챙

`u ∈ [-1, 1]`은 좌우, `q ∈ [0, 1]`은 크라운 연결부에서 앞쪽 끝까지다.

```text
a = sqrt(max(0, 1 - u^2))
x = u × (1 + 0.045 × q × a)
y = 0.26 - 0.36 × u^2
    - (0.17 + 0.50 × u^2) × q × a
    + 0.025 × q^2 × a
z = 1.05 × a + 0.86 × a × q
```

`q = 0`에서 `x = u`, `y = 0.26 - 0.36u²`, `z = 1.05√(1-u²)`가 된다. 이는 크라운 앞쪽 하단의 같은 위치와 맞는다. **크라운 하단을 변경하면 이 연결식도 함께 바꾼다.**

챙의 전방 낙차와 좌우 굽힘을 별도로 조정한다. 좌우 끝에서 돌출 길이가 줄어들도록 해야 사각형 조각 같은 모서리가 생기지 않는다. 정면에서 중앙과 양 끝의 높이 차이, 측면에서 길이와 낙차를 함께 확인한다.

### 기준 조립값

| 항목 | 현재 구현값 | 재사용 시 판단 |
|---|---|---|
| 패널 | 6개, 각 60도 | 실제 패널 수에 맞춤 |
| 패널 격자 | 32 × 40 | 접사 품질·성능에 맞춤 |
| 챙 위 격자 | 96 × 36 | 곡률을 보존하며 조절 |
| 챙 아래 | Y 방향 -0.018 오프셋 | 기준 사례의 얇은 두께 표현; 정확한 솔리드 아님 |
| 챙 스티치 | 7줄 | 사진의 수·간격 확인 |
| NY 패치 | 폭 0.35, 높이 0.36, Y 0.44~0.80 | 새 로고 비율·위치로 교체 |
| 뒤 패치 | 폭 0.34, 높이 0.215 | 실제 뒤 장식 유무 확인 |
| 원단 UV | 패널 2.8 × 4, 챙 6.4 × 3.5 | 면별 늘어짐과 결 방향 확인 |
| 원단 추가 반복 | 1.35 × 1.35 | 원본 크롭과 합쳐 최종 결 크기를 결정 |
| 결 대비 보정 | 채널 평균 + (원래 값 - 평균) × 0.65 | 이 사진에서만 채택; 보편 공식 아님 |

로고는 표면 높이에 맞는 `t`를 이분 탐색해 곡면에 배치한다. 기준 구현은 24회 탐색과 약 0.008~0.011의 작은 바깥 오프셋을 사용했다. 프로젝트 단위가 바뀌면 오프셋도 함께 바꾸고, 확대·측면에서 들뜸과 Z-fighting을 확인한다.

## 7. 원본 사진과 파생물의 대응

기준 SKU는 `M21N3ACP7701N`이다. 원본 폴더는 `mlb_shop_dev/reference/caps-detail/M21N3ACP7701N/`이며, 다른 프로젝트에서는 이 경로를 요구하지 않는다.

| 원본 | 역할 | 파생물 |
|---|---|---|
| `gallery-0.png` | 사선 형태·전체 인상 | 비교용 사선 사진 |
| `gallery-4.png` | 측면 실루엣 | 비교용 측면 사진 |
| `gallery-5.jpg` | 정면·NY 자수 | `ny.png`, 정면 비교 사진 |
| `gallery-6.jpg` | 개구부·스트랩·뒤 로고 | `rear.png`, 뒷면 비교 사진 |
| `gallery-7.jpg` | 원단 확대 | `twill.png`, `twill-refined.png` |
| `gallery-8.jpg` | 내부 구조 | 내부 형태·테이프의 관찰 근거 |

정확한 크롭 좌표·알파 임계값·색 보정은 [assets.mjs](cap_reference_viewer/scripts/assets.mjs)에 기록돼 있다. 좌표는 원본 해상도의 픽셀 좌표다. 축소된 화면 좌표를 원본에 그대로 사용하지 않는다.

## 8. 검수 순서와 완료 기준

### 검수 순서

1. **형상:** 정면·측면·사선에서 크라운 비율, 챙 아치와 연결부를 원본과 비교한다.
2. **재질:** 원단 확대를 실제 고해상도 디테일 사진과 비교한다. 작게 축소한 이미지 모음만 보고 직조 크기를 판단하지 않는다.
3. **전체 구조:** 뒷면·안쪽·반대 측면·위쪽으로 돌려 틈, 중복 로고, 부유 패치, 원치 않는 투명면을 확인한다.
4. **조작:** 실제 브라우저에서 드래그, 확대, 초기화, 모든 시점·원본 선택 버튼을 누른다.
5. **화면:** 최소한 작은 화면과 데스크톱을 검사한다. 기준 사례는 375 / 768 / 1280 px를 사용했다.
6. **기술 상태:** 타입·빌드 검사, 텍스처 로딩, 콘솔 오류, 잘린 화면과 빈 캡처를 확인한다.
7. **수정 후 재검증:** 수정한 소스보다 새로운 캡처를 남긴다. 적용 환경에서 요구하는 독립 검토가 있다면 현재 결과로 수행한다.

정면 사진과 렌더를 물체 경계 기준으로 크롭하면 비율 비교에 도움이 된다. 픽셀 diff는 촬영 배경·노출·원근 차이에도 크게 반응하므로 낮거나 높은 점수만으로 형상·재질 품질을 판정하지 않는다. 관찰 방향을 맞춘 직접 비교가 우선이다.

### 완료 체크리스트

- [ ] 원단 색·직조·로고가 지정된 상품의 정체성을 유지한다.
- [ ] 정면과 측면 비율을 모두 확인했다.
- [ ] 크라운·챙 연결부에 눈에 띄는 틈이 없다.
- [ ] 뒤 개구부와 스트랩이 회전해도 실제 구조로 보인다.
- [ ] 로고가 거울상·뒤집힘·중복·공중 부유 상태가 아니다.
- [ ] 원단이 굵은 골지나 인공적인 격자처럼 보이지 않는다.
- [ ] 사진에 근거 없는 광택을 추가하지 않았다.
- [ ] 확대·회전·초기화와 제공한 모든 시점이 실제로 동작한다.
- [ ] 모바일에서 모델·버튼·한국어가 잘리지 않는다.
- [ ] 현재 빌드의 캡처와 오류 검사 결과가 있다.
- [ ] 원본이 보존됐고 사진 사용 부분·추정 부분·생략 부분을 기록했다.

## 9. 실제로 겪은 실패와 재발 방지

| 증상 | 확인된 원인 또는 점검 지점 | 작업 |
|---|---|---|
| 모자 로딩 실패, 곡선 내부 오류 | 경계에서 음수의 소수 거듭제곱으로 NaN 생성 | 입력 범위 제한 후 동일 브라우저 동작 재실행 |
| 크라운이 뾰족하거나 꼭대기가 울퉁불퉁 | 하단 보정이 꼭대기까지 영향을 줌 | 위쪽에서 보정이 사라지도록 감쇠 |
| 크라운이 좁고 높아 보임 | 단순 돔 반경 분포가 상품과 다름 | 높이뿐 아니라 어깨 폭을 조절하는 지수 수정 |
| 챙과 본체 사이 흰 틈 | 서로 다른 하단 높이식 | 경계를 같은 식으로 계산 |
| 챙이 사각형·판 조각처럼 보임 | 좌우 끝에서도 전방 길이가 유지됨 | 끝으로 갈수록 연장 길이를 축소 |
| 원단이 코듀로이·굵은 골처럼 보임 | 결의 크기·대비·반복이 맞지 않음 | 원본 접사와 비교해 밀도·대비·필터링 조정 |
| 뒷면 로고가 떠 보임 | 정면 표면식을 그대로 뒤집음 | 실제 뒷면 곡면에서 위치 재계산 |
| 모바일에서 모자 일부가 잘림 | 가로세로 비율만 갱신하고 프레이밍은 유지 | 화면 크기에 맞춰 줌·거리·여백 재계산 |

## 10. 다른 앱에 재사용할 때

같은 모자는 모자 생성 코드와 실제 사용 텍스처를 재사용하고 대상 앱의 카메라·조명·배치에 연결한다. 다른 모자는 생성 구조를 출발점으로 삼되 SKU, 사진 경로, 곡면 파라미터, 로고, 버클, 내부 구조를 교체한다.

- 현재 `materials()`는 URL을 내부에 고정한다. 여러 상품을 지원하려면 에셋 URL과 재질 설정을 입력으로 받도록 바꾼다.
- 현재 `createCap()`은 특정 SKU와 치수가 고정돼 있다. 다품종용 파라미터 API는 아직 구현되지 않았다.
- 같은 상품을 여러 개 놓을 때에는 원본 사진과 geometry/material/texture를 재사용할 수 있게 수명 관리를 설계한다. 공유 자원을 개별 모자 제거 때 폐기하지 않는다.
- 화면에서 크게 보이는 모델의 밀도를 그대로 대량 배치하지 않는다. 대상 장면의 실제 모자 개수·거리에서 메모리와 프레임 성능을 확인한다.
- 단독 뷰어의 합격이 매장 통합·착용·변형·GLB 내보내기의 합격은 아니다. 해당 요청이 있으면 그 표면에서 별도로 검증한다.
- 기존 서버 포트와 리스너를 확인하고 빈 포트로 실행한다. `5187`은 이번 사례의 주소이며 다른 프로젝트의 고정값이 아니다.

### 이번 구현에서 아직 보장하지 않는 것

실측 치수, 물리적으로 복원된 PBR 맵, 자수 실의 실제 입체 구조, 제조용 패턴·닫힌 솔리드, 리깅·착용 변형, 자동 GLB 내보내기, 다품종 설정 API는 포함하지 않았다. 내부 테이프 글자는 재구성했으며 작은 내부 케어 라벨은 생략했다. 환기구는 토러스와 어두운 원형 면을 조합한 표현으로, 천을 실제로 관통시킨 구멍은 아니다.

## 11. 재사용 요청 예시

> `cap_real_skill.md`를 기준으로 `<reference-folder>`의 모자를 `<output-folder>`에 만들어줘. 기존 모자 틀은 출발점으로 사용하고, 새 상품의 정면·측면 비율과 원단·자수·뒷면 구조를 맞춰줘. 원본을 보존하고 단독 Three.js 화면에서 회전·확대·안쪽까지 검증해줘. 실측값이 없는 부분과 생략한 디테일은 기록해줘.

작업을 전달할 때에는 **실행 주소 또는 실행 명령, 소스 경로, 사용한 사진과 재질 방식, 확인한 시점, 남은 한계**를 간단히 함께 보고한다.
