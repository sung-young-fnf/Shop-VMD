# --- dependencies / build output ---
node_modules/
dist/
.vite/
*.tsbuildinfo

# --- harness verification screenshots ---
# 재생성 가능한 검증 산출물(스크린샷 2,019장 / 약 490MB). 스펙과 코드로 다시 만들 수 있어 제외.
evidence/

# --- 대용량 원본 도면 PDF ---
# ★M_성수점_준공도면_*.pdf 는 부당 59.5MB. GitHub 50MB 경고 임계를 넘어 제외.
# 도면 내용은 reference/drawing_captures/ 의 PNG 캡처로 레포에 남아 있음.
*.pdf

# --- 에이전트 / 툴 로컬 상태 ---
.omo/
.img2/
.img2threejs/
tmp/

# --- logs ---
*.log
npm-debug.log*
pnpm-debug.log*

# --- secrets (configs/*.env 템플릿은 시크릿이 없어 커밋 대상) ---
.env
.env.*

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- 신규 고해상도 갤러리 원본 (2.3GB) ---
# headwear-detail 1,188MB / clothes-detail 1,147MB. src/ 에서 참조하지 않고
# scripts/discover-headwear.mjs, discover-clothing-pairs.mjs 로 재수집 가능한
# 스크레이핑 원본이라 제외. 앱이 실제로 쓰는 컷은 public/products/ 에 커밋됨.
mlb_shop_dev/reference/headwear-detail/
mlb_shop_dev/reference/clothes-detail/
